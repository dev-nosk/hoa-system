$(document).ready(function() {
    var $select = $('.select-category');
    var dataLoaded = false;
   
    // Initialize Select2 (even with no options yet)
    $select.select2({
        placeholder: 'Select a category',
        allowClear: true,
        width: '100%'
    });

 

});